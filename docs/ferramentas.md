# Ferramentas

CVM: Processos Administrativos Sancionadores expõe 1 ferramenta (todas somente leitura).

### 1. `cvm_processo_administrativo_consultar`
**Input**: `numero_processo` (opcional), `nome` (opcional), `cpf` (opcional), `cnpj` (opcional)

CVM: Processos Administrativos Sancionadores, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar CVM: Processos Administrativos Sancionadores
```
